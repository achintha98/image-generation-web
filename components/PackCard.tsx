import React from "react";

export interface TPack {
  name: string;
  imageUrl: string;
  description: string;
}

const PackCard = (prop: TPack) => {
  return <div></div>;
};

export default PackCard;
